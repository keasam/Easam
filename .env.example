# Vercel / Prisma Postgres database connection
# Set the real value in Vercel Environment Variables.
DATABASE_URL=

# Google Gemini API (server-side only — never use NEXT_PUBLIC_)
# Create a current Gemini API key and add the real value in Vercel.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
